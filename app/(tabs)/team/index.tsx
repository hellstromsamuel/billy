import { SafeAreaView, ScrollView } from "react-native";

export default function TeamScreen() {
  return (
    <SafeAreaView>
      <ScrollView className="h-full px-2"></ScrollView>
    </SafeAreaView>
  );
}
